(ns stackoverflow.core-test
  (:require [clojure.test :refer :all]
            [stackoverflow.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
  (print "start adding test")
    (is (= 0 1))))
